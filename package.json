{
  "name": "jeu-de-dame",
  "private": true,
  "scripts": {
    "build": "cd prototype-front && npm ci && npm run build && cd ../nuxt_IA && npm ci && npm run build",
    "start": "bash start.sh"
  }
}
